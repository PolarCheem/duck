local objectsToSave = {
"SetEquipped",
"SetGraphic",
"SetInformation",
"SetKeybind",
"SetMisc",
"SetToolOrder",
"Emote",
}

local function deleteRemovedObjects()
for _, object in pairs(ReplicatedStorage.Remotes:GetChildren()) do
local found = false
for _, name in pairs(objectsToSave) do
if object.Name == name then
found = true
break
end
end
if not found then
object:Destroy()
end
end
end

deleteRemovedObjects()