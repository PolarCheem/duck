local player = game:GetService("Players").LocalPlayer
local TSize = 25

while true do
player.Character.CatchGlove.Size = Vector3.new(TSize, TSize, TSize)
player.Character.CatchGlove.Size = Vector3.new(TSize, TSize, TSize)
wait(0)
end