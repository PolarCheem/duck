local OrionLib = loadstring(game:HttpGet(('https://raw.githubusercontent.com/shlexware/Orion/main/source')))()
local Window = OrionLib:MakeWindow({Name = "Fake Animation | Veux Hub", HidePremium = false, SaveConfig = true, ConfigFolder = "OrionTest"})
loadstring(game:HttpGet("https://raw.githubusercontent.com/PolarCheem/duck/main/jujutsu%20shenanigans"))()

OrionLib:MakeNotification({
	Name = "Thank You",
	Content = "Made by @.ono101|@LocalGoodduck",
	Image = "rbxassetid://4483345998",
	Time = 10
})

local Tab1 = Window:MakeTab({
	Name = "itadori",
	Icon = "rbxassetid://4483345998",
	PremiumOnly = false
})

Tab1:AddButton({
	Name = "cursed Strike|victim",
	Callback = function()
      		local Players = game:GetService("Players")
local player = Players.LocalPlayer

local animationId = "rbxassetid://16872047350"
local animation = Instance.new("Animation")
animation.AnimationId = animationId

local function playAnimation(character)
    local humanoid = character:FindFirstChildOfClass("Humanoid")
    if humanoid then
        local animator = humanoid:FindFirstChildOfClass("Animator") or Instance.new("Animator", humanoid)
        local animationTrack = animator:LoadAnimation(animation)
        animationTrack:Play()
    end
end

player.CharacterAdded:Connect(playAnimation)
if player.Character then
    playAnimation(player.Character)
      end
  	end    
})

Tab1:AddButton({
	Name = "Crushing Blow",
	Callback = function()
      		local Players = game:GetService("Players")
local player = Players.LocalPlayer

local animationId = "rbxassetid://15432272268"
local animation = Instance.new("Animation")
animation.AnimationId = animationId

local function playAnimation(character)
    local humanoid = character:FindFirstChildOfClass("Humanoid")
    if humanoid then
        local animator = humanoid:FindFirstChildOfClass("Animator") or Instance.new("Animator", humanoid)
        local animationTrack = animator:LoadAnimation(animation)
        animationTrack:Play()
    end
end

player.CharacterAdded:Connect(playAnimation)
if player.Character then
    playAnimation(player.Character)
      end
  	end    
})

Tab1:AddButton({
	Name = "Cursed strike",
	Callback = function()
      local Players = game:GetService("Players")
local player = Players.LocalPlayer

local animationId = "rbxassetid://15434683426"
local animation = Instance.new("Animation")
animation.AnimationId = animationId

local function playAnimation(character)
    local humanoid = character:FindFirstChildOfClass("Humanoid")
    if humanoid then
        local animator = humanoid:FindFirstChildOfClass("Animator") or Instance.new("Animator", humanoid)
        local animationTrack = animator:LoadAnimation(animation)
        animationTrack:Play()
    end
end

player.CharacterAdded:Connect(playAnimation)
if player.Character then
    playAnimation(player.Character)
      end
      end    
})

Tab1:AddButton({
	Name = "Maji Kick",
	Callback = function()
      local Players = game:GetService("Players")
local player = Players.LocalPlayer

local animationId = "rbxassetid://14841505475"
local animation = Instance.new("Animation")
animation.AnimationId = animationId

local function playAnimation(character)
    local humanoid = character:FindFirstChildOfClass("Humanoid")
    if humanoid then
        local animator = humanoid:FindFirstChildOfClass("Animator") or Instance.new("Animator", humanoid)
        local animationTrack = animator:LoadAnimation(animation)
        animationTrack:Play()
    end
end

player.CharacterAdded:Connect(playAnimation)
if player.Character then
    playAnimation(player.Character)
      end
    end    
})

Tab1:AddButton({
	Name = "ultimate",
	Callback = function()
      local Players = game:GetService("Players")
local player = Players.LocalPlayer

local animationId = "rbxassetid://16663212523"
local animation = Instance.new("Animation")
animation.AnimationId = animationId

local function playAnimation(character)
    local humanoid = character:FindFirstChildOfClass("Humanoid")
    if humanoid then
        local animator = humanoid:FindFirstChildOfClass("Animator") or Instance.new("Animator", humanoid)
        local animationTrack = animator:LoadAnimation(animation)
        animationTrack:Play()
    end
end

player.CharacterAdded:Connect(playAnimation)
if player.Character then
    playAnimation(player.Character)
      end
    end    
})

Tab1:AddButton({
	Name = "DomainWarn",
	Callback = function()
      local Players = game:GetService("Players")
local player = Players.LocalPlayer

local animationId = "rbxassetid://15488163846"
local animation = Instance.new("Animation")
animation.AnimationId = animationId

local function playAnimation(character)
    local humanoid = character:FindFirstChildOfClass("Humanoid")
    if humanoid then
        local animator = humanoid:FindFirstChildOfClass("Animator") or Instance.new("Animator", humanoid)
        local animationTrack = animator:LoadAnimation(animation)
        animationTrack:Play()
    end
end

player.CharacterAdded:Connect(playAnimation)
if player.Character then
    playAnimation(player.Character)
      end
    end    
})

Tab1:AddButton({
	Name = "Flame Arrow",
	Callback = function()
      local Players = game:GetService("Players")
local player = Players.LocalPlayer

local animationId = "rbxassetid://15934318270"
local animation = Instance.new("Animation")
animation.AnimationId = animationId

local function playAnimation(character)
    local humanoid = character:FindFirstChildOfClass("Humanoid")
    if humanoid then
        local animator = humanoid:FindFirstChildOfClass("Animator") or Instance.new("Animator", humanoid)
        local animationTrack = animator:LoadAnimation(animation)
        animationTrack:Play()
    end
end

player.CharacterAdded:Connect(playAnimation)
if player.Character then
    playAnimation(player.Character)
      end
    end    
})

Tab1:AddButton({
	Name = "Rush",
	Callback = function()
      local Players = game:GetService("Players")
local player = Players.LocalPlayer

local animationId = "rbxassetid://15984964491"
local animation = Instance.new("Animation")
animation.AnimationId = animationId

local function playAnimation(character)
    local humanoid = character:FindFirstChildOfClass("Humanoid")
    if humanoid then
        local animator = humanoid:FindFirstChildOfClass("Animator") or Instance.new("Animator", humanoid)
        local animationTrack = animator:LoadAnimation(animation)
        animationTrack:Play()
    end
end

player.CharacterAdded:Connect(playAnimation)
if player.Character then
    playAnimation(player.Character)
      end
    end    
})

Tab1:AddButton({
	Name = "Rush Hit",
	Callback = function()
      local Players = game:GetService("Players")
local player = Players.LocalPlayer

local animationId = "rbxassetid://16827230331"
local animation = Instance.new("Animation")
animation.AnimationId = animationId

local function playAnimation(character)
    local humanoid = character:FindFirstChildOfClass("Humanoid")
    if humanoid then
        local animator = humanoid:FindFirstChildOfClass("Animator") or Instance.new("Animator", humanoid)
        local animationTrack = animator:LoadAnimation(animation)
        animationTrack:Play()
    end
end

player.CharacterAdded:Connect(playAnimation)
if player.Character then
    playAnimation(player.Character)
      end
    end    
})

Tab1:AddButton({
	Name = "cleave",
	Callback = function()
      local Players = game:GetService("Players")
local player = Players.LocalPlayer

local animationId = "rbxassetid://16638412783"
local animation = Instance.new("Animation")
animation.AnimationId = animationId

local function playAnimation(character)
    local humanoid = character:FindFirstChildOfClass("Humanoid")
    if humanoid then
        local animator = humanoid:FindFirstChildOfClass("Animator") or Instance.new("Animator", humanoid)
        local animationTrack = animator:LoadAnimation(animation)
        animationTrack:Play()
    end
end

player.CharacterAdded:Connect(playAnimation)
if player.Character then
    playAnimation(player.Character)
      end
    end    
})

Tab1:AddButton({
	Name = "MalevolentShrine",
	Callback = function()
      local Players = game:GetService("Players")
local player = Players.LocalPlayer

local animationId = "rbxassetid://15489784732"
local animation = Instance.new("Animation")
animation.AnimationId = animationId

local function playAnimation(character)
    local humanoid = character:FindFirstChildOfClass("Humanoid")
    if humanoid then
        local animator = humanoid:FindFirstChildOfClass("Animator") or Instance.new("Animator", humanoid)
        local animationTrack = animator:LoadAnimation(animation)
        animationTrack:Play()
    end
end

player.CharacterAdded:Connect(playAnimation)
if player.Character then
    playAnimation(player.Character)
      end
    end    
})

Tab1:AddButton({
	Name = "Dismantle",
	Callback = function()
      local Players = game:GetService("Players")
local player = Players.LocalPlayer

local animationId = "rbxassetid://15292037778"
local animation = Instance.new("Animation")
animation.AnimationId = animationId

local function playAnimation(character)
    local humanoid = character:FindFirstChildOfClass("Humanoid")
    if humanoid then
        local animator = humanoid:FindFirstChildOfClass("Animator") or Instance.new("Animator", humanoid)
        local animationTrack = animator:LoadAnimation(animation)
        animationTrack:Play()
    end
end

player.CharacterAdded:Connect(playAnimation)
if player.Character then
    playAnimation(player.Character)
      end
    end    
})

Tab1:AddButton({
	Name = "DivergentFist",
	Callback = function()
      local Players = game:GetService("Players")
local player = Players.LocalPlayer

local animationId = "rbxassetid://14771588355"
local animation = Instance.new("Animation")
animation.AnimationId = animationId

local function playAnimation(character)
    local humanoid = character:FindFirstChildOfClass("Humanoid")
    if humanoid then
        local animator = humanoid:FindFirstChildOfClass("Animator") or Instance.new("Animator", humanoid)
        local animationTrack = animator:LoadAnimation(animation)
        animationTrack:Play()
    end
end

player.CharacterAdded:Connect(playAnimation)
if player.Character then
    playAnimation(player.Character)
      end
    end    
})

Tab1:AddButton({
	Name = "SlaughterDemon",
	Callback = function()
      local Players = game:GetService("Players")
local player = Players.LocalPlayer

local animationId = "rbxassetid://16735568688"
local animation = Instance.new("Animation")
animation.AnimationId = animationId

local function playAnimation(character)
    local humanoid = character:FindFirstChildOfClass("Humanoid")
    if humanoid then
        local animator = humanoid:FindFirstChildOfClass("Animator") or Instance.new("Animator", humanoid)
        local animationTrack = animator:LoadAnimation(animation)
        animationTrack:Play()
    end
end

player.CharacterAdded:Connect(playAnimation)
if player.Character then
    playAnimation(player.Character)
      end
    end    
})

local Tab1 = Window:MakeTab({
	Name = "megumi 70% ",
	Icon = "rbxassetid://4483345998",
	PremiumOnly = false
})

Tab1:AddButton({
	Name = "Great serpent",
	Callback = function()
      		local Players = game:GetService("Players")
local player = Players.LocalPlayer

local animationId = "rbxassetid://17656896351"
local animation = Instance.new("Animation")
animation.AnimationId = animationId

local function playAnimation(character)
    local humanoid = character:FindFirstChildOfClass("Humanoid")
    if humanoid then
        local animator = humanoid:FindFirstChildOfClass("Animator") or Instance.new("Animator", humanoid)
        local animationTrack = animator:LoadAnimation(animation)
        animationTrack:Play()
    end
end

player.CharacterAdded:Connect(playAnimation)
if player.Character then
    playAnimation(player.Character)
      end
  	end    
    })

    Tab1:AddButton({
	Name = "shadow Swarm Run",
	Callback = function()
      		local Players = game:GetService("Players")
local player = Players.LocalPlayer

local animationId = "rbxassetid://17588190031"
local animation = Instance.new("Animation")
animation.AnimationId = animationId

local function playAnimation(character)
    local humanoid = character:FindFirstChildOfClass("Humanoid")
    if humanoid then
        local animator = humanoid:FindFirstChildOfClass("Animator") or Instance.new("Animator", humanoid)
        local animationTrack = animator:LoadAnimation(animation)
        animationTrack:Play()
    end
end

player.CharacterAdded:Connect(playAnimation)
if player.Character then
    playAnimation(player.Character)
      end
  	end    
    })

Tab1:AddButton({
	Name = "shadowSwarm 2",
	Callback = function()
      		local Players = game:GetService("Players")
local player = Players.LocalPlayer

local animationId = "rbxassetid://17588171119"
local animation = Instance.new("Animation")
animation.AnimationId = animationId

local function playAnimation(character)
    local humanoid = character:FindFirstChildOfClass("Humanoid")
    if humanoid then
        local animator = humanoid:FindFirstChildOfClass("Animator") or Instance.new("Animator", humanoid)
        local animationTrack = animator:LoadAnimation(animation)
        animationTrack:Play()
    end
end

player.CharacterAdded:Connect(playAnimation)
if player.Character then
    playAnimation(player.Character)
      end
  	end    
    })

Tab1:AddButton({
	Name = "shadow Swarm 1",
	Callback = function()
      		local Players = game:GetService("Players")
local player = Players.LocalPlayer

local animationId = "rbxassetid://17588168513"
local animation = Instance.new("Animation")
animation.AnimationId = animationId

local function playAnimation(character)
    local humanoid = character:FindFirstChildOfClass("Humanoid")
    if humanoid then
        local animator = humanoid:FindFirstChildOfClass("Animator") or Instance.new("Animator", humanoid)
        local animationTrack = animator:LoadAnimation(animation)
        animationTrack:Play()
    end
end

player.CharacterAdded:Connect(playAnimation)
if player.Character then
    playAnimation(player.Character)
      end
  	end    
    })

Tab1:AddButton({
	Name = "shadow Swarm Hit",
	Callback = function()
      		local Players = game:GetService("Players")
local player = Players.LocalPlayer

local animationId = "rbxassetid://17588173341"
local animation = Instance.new("Animation")
animation.AnimationId = animationId

local function playAnimation(character)
    local humanoid = character:FindFirstChildOfClass("Humanoid")
    if humanoid then
        local animator = humanoid:FindFirstChildOfClass("Animator") or Instance.new("Animator", humanoid)
        local animationTrack = animator:LoadAnimation(animation)
        animationTrack:Play()
    end
end

player.CharacterAdded:Connect(playAnimation)
if player.Character then
    playAnimation(player.Character)
      end
  	end    
    })

Tab1:AddButton({
	Name = "Mahoraga Summon",
	Callback = function()
      local Players = game:GetService("Players")
local player = Players.LocalPlayer

local animationId = "rbxassetid://17551479073"
local animation = Instance.new("Animation")
animation.AnimationId = animationId

local function playAnimation(character)
    local humanoid = character:FindFirstChildOfClass("Humanoid")
    if humanoid then
        local animator = humanoid:FindFirstChildOfClass("Animator") or Instance.new("Animator", humanoid)
        local animationTrack = animator:LoadAnimation(animation)
        animationTrack:Play()
    end
end

player.CharacterAdded:Connect(playAnimation)
if player.Character then
    playAnimation(player.Character)
      end
    end    
})
Tab1:AddButton({
	Name = "Mahoraga Use",
	Callback = function()
      local Players = game:GetService("Players")
local player = Players.LocalPlayer

local animationId = "rbxassetid://17549011110"
local animation = Instance.new("Animation")
animation.AnimationId = animationId

local function playAnimation(character)
    local humanoid = character:FindFirstChildOfClass("Humanoid")
    if humanoid then
        local animator = humanoid:FindFirstChildOfClass("Animator") or Instance.new("Animator", humanoid)
        local animationTrack = animator:LoadAnimation(animation)
        animationTrack:Play()
    end
end

player.CharacterAdded:Connect(playAnimation)
if player.Character then
    playAnimation(player.Character)
      end
    end    
})

Tab1:AddButton({
	Name = "Divine Dog",
	Callback = function()
      local Players = game:GetService("Players")
local player = Players.LocalPlayer

local animationId = "rbxassetid://17396322509"
local animation = Instance.new("Animation")
animation.AnimationId = animationId

local function playAnimation(character)
    local humanoid = character:FindFirstChildOfClass("Humanoid")
    if humanoid then
        local animator = humanoid:FindFirstChildOfClass("Animator") or Instance.new("Animator", humanoid)
        local animationTrack = animator:LoadAnimation(animation)
        animationTrack:Play()
    end
end

player.CharacterAdded:Connect(playAnimation)
if player.Character then
    playAnimation(player.Character)
      end
    end    
})

Tab1:AddButton({
	Name = "Domain Warn",
	Callback = function()
      local Players = game:GetService("Players")
local player = Players.LocalPlayer

local animationId = "rbxassetid://17284846608"
local animation = Instance.new("Animation")
animation.AnimationId = animationId

local function playAnimation(character)
    local humanoid = character:FindFirstChildOfClass("Humanoid")
    if humanoid then
        local animator = humanoid:FindFirstChildOfClass("Animator") or Instance.new("Animator", humanoid)
        local animationTrack = animator:LoadAnimation(animation)
        animationTrack:Play()
    end
end

player.CharacterAdded:Connect(playAnimation)
if player.Character then
    playAnimation(player.Character)
      end
    end    
})

Tab1:AddButton({
	Name = "shadow Swarm",
	Callback = function()
      local Players = game:GetService("Players")
local player = Players.LocalPlayer

local animationId = "rbxassetid://17588165564"
local animation = Instance.new("Animation")
animation.AnimationId = animationId

local function playAnimation(character)
    local humanoid = character:FindFirstChildOfClass("Humanoid")
    if humanoid then
        local animator = humanoid:FindFirstChildOfClass("Animator") or Instance.new("Animator", humanoid)
        local animationTrack = animator:LoadAnimation(animation)
        animationTrack:Play()
    end
end

player.CharacterAdded:Connect(playAnimation)
if player.Character then
    playAnimation(player.Character)
      end
    end    
})

Tab1:AddButton({
	Name = "ultimate",
	Callback = function()
      local Players = game:GetService("Players")
local player = Players.LocalPlayer

local animationId = "rbxassetid://17243305851"
local animation = Instance.new("Animation")
animation.AnimationId = animationId

local function playAnimation(character)
    local humanoid = character:FindFirstChildOfClass("Humanoid")
    if humanoid then
        local animator = humanoid:FindFirstChildOfClass("Animator") or Instance.new("Animator", humanoid)
        local animationTrack = animator:LoadAnimation(animation)
        animationTrack:Play()
    end
end

player.CharacterAdded:Connect(playAnimation)
if player.Character then
    playAnimation(player.Character)
      end
    end    
})

Tab1:AddButton({
	Name = "Toad",
	Callback = function()
      local Players = game:GetService("Players")
local player = Players.LocalPlayer

local animationId = "rbxassetid://17263623586"
local animation = Instance.new("Animation")
animation.AnimationId = animationId

local function playAnimation(character)
    local humanoid = character:FindFirstChildOfClass("Humanoid")
    if humanoid then
        local animator = humanoid:FindFirstChildOfClass("Animator") or Instance.new("Animator", humanoid)
        local animationTrack = animator:LoadAnimation(animation)
        animationTrack:Play()
    end
end

player.CharacterAdded:Connect(playAnimation)
if player.Character then
    playAnimation(player.Character)
      end
    end    
})

Tab1:AddButton({
	Name = "Nue",
	Callback = function()
      local Players = game:GetService("Players")
local player = Players.LocalPlayer

local animationId = "rbxassetid://17324258817"
local animation = Instance.new("Animation")
animation.AnimationId = animationId

local function playAnimation(character)
    local humanoid = character:FindFirstChildOfClass("Humanoid")
    if humanoid then
        local animator = humanoid:FindFirstChildOfClass("Animator") or Instance.new("Animator", humanoid)
        local animationTrack = animator:LoadAnimation(animation)
        animationTrack:Play()
    end
end

player.CharacterAdded:Connect(playAnimation)
if player.Character then
    playAnimation(player.Character)
      end
    end    
})

Tab1:AddButton({
	Name = "shadow",
	Callback = function()
      local Players = game:GetService("Players")
local player = Players.LocalPlayer

local animationId = "rbxassetid://17110997686"
local animation = Instance.new("Animation")


local function playAnimation(character)
    local humanoid = character:FindFirstChildOfClass("Humanoid")
    if humanoid then
        local animator = humanoid:FindFirstChildOfClass("Animator") or Instance.new("Animator", humanoid)
        local animationTrack = animator:LoadAnimation(animation)
        animationTrack:Play()
    end
end

player.CharacterAdded:Connect(playAnimation)
if player.Character then
    playAnimation(player.Character)
      end
    end    
})

local Tab2 = Window:MakeTab({
	Name = "Gojo",
	Icon = "rbxassetid://4483345998",
	PremiumOnly = false
})

local Section = Tab2:AddSection({
	Name = "Base"
})

Tab2:AddButton({
	Name = "Lapis Blue",
	Callback = function()
local Players = game:GetService("Players")
local player = Players.LocalPlayer

local animationId = "rbxassetid://14702384584"
local animation = Instance.new("Animation")
animation.AnimationId = animationId

local function playAnimation(character)
    local humanoid = character:FindFirstChildOfClass("Humanoid")
    if humanoid then
        local animator = humanoid:FindFirstChildOfClass("Animator") or Instance.new("Animator", humanoid)
        local animationTrack = animator:LoadAnimation(animation)
        animationTrack:Play()
    end
end

player.CharacterAdded:Connect(playAnimation)
if player.Character then
    playAnimation(player.Character)
      end
  	end    
})

Tab2:AddButton({
	Name = "Reversal Red",
	Callback = function()
local Players = game:GetService("Players")
local player = Players.LocalPlayer

local animationId = "rbxassetid://14135387764"
local animation = Instance.new("Animation")
animation.AnimationId = animationId

local function playAnimation(character)
    local humanoid = character:FindFirstChildOfClass("Humanoid")
    if humanoid then
        local animator = humanoid:FindFirstChildOfClass("Animator") or Instance.new("Animator", humanoid)
        local animationTrack = animator:LoadAnimation(animation)
        animationTrack:Play()
    end
end

player.CharacterAdded:Connect(playAnimation)
if player.Character then
    playAnimation(player.Character)
      end
  	end    
})

Tab2:AddButton({
	Name = "Barrage",
	Callback = function()
local Players = game:GetService("Players")
local player = Players.LocalPlayer

local animationId = "rbxassetid://15042750274"
local animation = Instance.new("Animation")
animation.AnimationId = animationId

local function playAnimation(character)
    local humanoid = character:FindFirstChildOfClass("Humanoid")
    if humanoid then
        local animator = humanoid:FindFirstChildOfClass("Animator") or Instance.new("Animator", humanoid)
        local animationTrack = animator:LoadAnimation(animation)
        animationTrack:Play()
    end
end

player.CharacterAdded:Connect(playAnimation)
if player.Character then
    playAnimation(player.Character)
      end
  	end    
})

Tab2:AddButton({
	Name = "Twinfold kick",
	Callback = function()
local Players = game:GetService("Players")
local player = Players.LocalPlayer

local animationId = "rbxassetid://16842286124"
local animation = Instance.new("Animation")
animation.AnimationId = animationId

local function playAnimation(character)
    local humanoid = character:FindFirstChildOfClass("Humanoid")
    if humanoid then
        local animator = humanoid:FindFirstChildOfClass("Animator") or Instance.new("Animator", humanoid)
        local animationTrack = animator:LoadAnimation(animation)
        animationTrack:Play()
    end
end

player.CharacterAdded:Connect(playAnimation)
if player.Character then
    playAnimation(player.Character)
      end
  	end    
})

Tab2:AddButton({
	Name = "Teleport",
	Callback = function()
local Players = game:GetService("Players")
local player = Players.LocalPlayer

local animationId = "rbxassetid://16028730554"
local animation = Instance.new("Animation")
animation.AnimationId = animationId

local function playAnimation(character)
    local humanoid = character:FindFirstChildOfClass("Humanoid")
    if humanoid then
        local animator = humanoid:FindFirstChildOfClass("Animator") or Instance.new("Animator", humanoid)
        local animationTrack = animator:LoadAnimation(animation)
        animationTrack:Play()
    end
end

player.CharacterAdded:Connect(playAnimation)
if player.Character then
    playAnimation(player.Character)
      end
  	end    
})

local Section = Tab2:AddSection({
	Name = "Ultimate"
})

Tab2:AddButton({
	Name = "Ultimate",
	Callback = function()
local Players = game:GetService("Players")
local player = Players.LocalPlayer

local animationId = "rbxassetid://14737876270"
local animation = Instance.new("Animation")
animation.AnimationId = animationId

local function playAnimation(character)
    local humanoid = character:FindFirstChildOfClass("Humanoid")
    if humanoid then
        local animator = humanoid:FindFirstChildOfClass("Animator") or Instance.new("Animator", humanoid)
        local animationTrack = animator:LoadAnimation(animation)
        animationTrack:Play()
    end
end

player.CharacterAdded:Connect(playAnimation)
if player.Character then
    playAnimation(player.Character)
      end
  	end    
})

Tab2:AddButton({
	Name = "DomainWarn",
	Callback = function()
local Players = game:GetService("Players")
local player = Players.LocalPlayer

local animationId = "rbxassetid://15170309180"
local animation = Instance.new("Animation")
animation.AnimationId = animationId

local function playAnimation(character)
    local humanoid = character:FindFirstChildOfClass("Humanoid")
    if humanoid then
        local animator = humanoid:FindFirstChildOfClass("Animator") or Instance.new("Animator", humanoid)
        local animationTrack = animator:LoadAnimation(animation)
        animationTrack:Play()
    end
end

player.CharacterAdded:Connect(playAnimation)
if player.Character then
    playAnimation(player.Character)
      end
  	end    
})

Tab2:AddButton({
	Name = "Blue Max",
	Callback = function()
local Players = game:GetService("Players")
local player = Players.LocalPlayer

local animationId = "rbxassetid://15290922156"
local animation = Instance.new("Animation")
animation.AnimationId = animationId

local function playAnimation(character)
    local humanoid = character:FindFirstChildOfClass("Humanoid")
    if humanoid then
        local animator = humanoid:FindFirstChildOfClass("Animator") or Instance.new("Animator", humanoid)
        local animationTrack = animator:LoadAnimation(animation)
        animationTrack:Play()
    end
end

player.CharacterAdded:Connect(playAnimation)
if player.Character then
    playAnimation(player.Character)
      end
  	end    
})

Tab2:AddButton({
	Name = "Red Max",
	Callback = function()
local Players = game:GetService("Players")
local player = Players.LocalPlayer

local animationId = "rbxassetid://17283946287"
local animation = Instance.new("Animation")
animation.AnimationId = animationId

local function playAnimation(character)
    local humanoid = character:FindFirstChildOfClass("Humanoid")
    if humanoid then
        local animator = humanoid:FindFirstChildOfClass("Animator") or Instance.new("Animator", humanoid)
        local animationTrack = animator:LoadAnimation(animation)
        animationTrack:Play()
    end
end

player.CharacterAdded:Connect(playAnimation)
if player.Character then
    playAnimation(player.Character)
      end
  	end    
})

Tab2:AddButton({
	Name = "Purple",
	Callback = function()
local Players = game:GetService("Players")
local player = Players.LocalPlayer

local animationId = "rbxassetid://14351933730"
local animation = Instance.new("Animation")
animation.AnimationId = animationId

local function playAnimation(character)
    local humanoid = character:FindFirstChildOfClass("Humanoid")
    if humanoid then
        local animator = humanoid:FindFirstChildOfClass("Animator") or Instance.new("Animator", humanoid)
        local animationTrack = animator:LoadAnimation(animation)
        animationTrack:Play()
    end
end

player.CharacterAdded:Connect(playAnimation)
if player.Character then
    playAnimation(player.Character)
      end
  	end    
})

Tab2:AddButton({
	Name = "Infinite Void",
	Callback = function()
local Players = game:GetService("Players")
local player = Players.LocalPlayer

local animationId = "rbxassetid://14801008075"
local animation = Instance.new("Animation")
animation.AnimationId = animationId

local function playAnimation(character)
    local humanoid = character:FindFirstChildOfClass("Humanoid")
    if humanoid then
        local animator = humanoid:FindFirstChildOfClass("Animator") or Instance.new("Animator", humanoid)
        local animationTrack = animator:LoadAnimation(animation)
        animationTrack:Play()
    end
end

player.CharacterAdded:Connect(playAnimation)
if player.Character then
    playAnimation(player.Character)
      end
  	end    
})

local Tab3 = Window:MakeTab({
	Name = "updates",
	Icon = "rbxassetid://4483345998",
	PremiumOnly = false
})

Tab3:AddParagraph("updates ?","GOJO AND MORE CHARACTERS ADDED WILL BE SOON")

Tab3.AddParagraph("Megumi 70% | Gojo 100%")

Tab3:AddParagraph("IMPORTANT","SAVE THIS SCRIPT ON YOUR EXECUTER ALL UPDATED WILL BE IN THIS SCRIPT")

Tab3:AddParagraph("credits","made by @.ono101|@Localgoodduck")
